{
  "name": "cocode-brand",
  "version": "1.0.0",
  "private": true,
  "description": "The cocode.dk family frame: <cocode-head> and <cocode-foot>, served at brand.cocode.dk",
  "license": "Apache-2.0",
  "scripts": {
    "build": "rollup -c && cat src/fonts.css dist/v1.css > dist/v1.tmp && mv dist/v1.tmp dist/v1.css && cp site/* dist/"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^16.0.3",
    "@stylexjs/rollup-plugin": "^0.19.1",
    "@stylexjs/stylex": "^0.19.1",
    "rollup": "^4.63.4"
  }
}
